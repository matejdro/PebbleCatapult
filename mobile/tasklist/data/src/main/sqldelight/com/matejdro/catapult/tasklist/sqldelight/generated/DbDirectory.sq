CREATE TABLE dbDirectory (
    id INTEGER PRIMARY KEY NOT NULL,
    title TEXT NOT NULL
);

selectAll:
SELECT * FROM dbDirectory;

insert:
INSERT INTO dbDirectory (title) VALUES ?;

delete:
DELETE FROM dbDirectory WHERE id = ?;

update:
REPLACE INTO dbDirectory VALUES ?;
